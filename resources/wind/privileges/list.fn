(sentinel::privilege-required admin)

(set list (db::table `
	SELECT
		p.privilege_id as id,
		p.name

	FROM ##privileges p
	ORDER BY p.privilege_id DESC
`))

(each i (list) (set i.name (strings.@enum.privileges.(i.name))))

(return (list))
