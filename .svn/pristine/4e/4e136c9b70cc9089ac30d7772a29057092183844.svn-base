(sentinel::privilege-required admin)

(shield::validate
	(shield::field id
		required: true
		pattern: integer
	)
)

(return (db::table `
	SELECT
		p.privilege_id as id,
		p.name
	FROM ##privileges p
	INNER JOIN ##user_privileges u ON u.privilege_id=p.privilege_id AND u.user_id={id}
`))
